# Documentation:
# https://doc.traefik.io/traefik/reference/routing-configuration/dynamic-configuration-methods/
# https://doc.traefik.io/traefik/reference/routing-configuration/http/tls/overview/

http:
  services:
    svc_romm-8080:
      loadBalancer:
        servers:
          - url: 'http://{{ env "APP_NAME_HOST" }}_romm:8080'
  routers:
    romm-8080:
      rule: 'Host(`romm.{{ env "APP_HOST" }}`)'
      priority: 1
      service: svc_romm-8080
      entryPoints: ["websecure"]
      middlewares:
        - "sts-headers"
        - "cors-policy"
      tls:
        certResolver: porkbun-api
